// import React from 'react'

// export default function Total(activities, categories) {
//   const sum = activities.reduce(
//     (acc, cur) => (cur.category === category ? (acc = acc + cur.hours) : acc),
//     0
//   )
//   return sum
// }
